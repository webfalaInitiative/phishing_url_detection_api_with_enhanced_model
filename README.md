# The LinkGuard Phishing Link Detection API

This API predicts whether a given URL is safe or unsafe using a trained Machine Learning model. It also stores every URL submitted into a database and provides endpoints to fetch and download predictions.
